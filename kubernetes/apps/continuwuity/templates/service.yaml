---
apiVersion: v1
kind: Service
metadata:
  name: continuwuity
  labels:
    app.kubernetes.io/name: continuwuity
spec:
  selector:
    app.kubernetes.io/name: continuwuity
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http

