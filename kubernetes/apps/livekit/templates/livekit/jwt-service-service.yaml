
---
apiVersion: v1
kind: Service
metadata:
  name: lk-jwt-service
  labels:
    app: lk-jwt-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: lk-jwt-service
