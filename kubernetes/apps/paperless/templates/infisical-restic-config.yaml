---
apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalAuth
metadata:
  name: infisical-auth
spec:
  infisicalConnectionRef:
    name: infisical-connection
    namespace: infisical-operator-system
  method: universal
  universal:
    clientIdRef:
      name: infisical-credentials
      namespace: paperless
      key: clientId
    clientSecretRef:
      name: infisical-credentials
      namespace: paperless
      key: clientSecret
---
apiVersion: secrets.infisical.com/v1beta1
kind: InfisicalStaticSecret
metadata:
  name: infisical-restic-config
spec:
  infisicalAuthRef:
    name: infisical-auth
    namespace: paperless
  syncOptions:
    refreshInterval: 60s
  sources:
    - projectSlug: paperless-l3ca
      environmentSlug: dev
      secretPath: /restic
  targets:
    - name: restic-config
      namespace: paperless
      kind: Secret
      creationPolicy: Owner
